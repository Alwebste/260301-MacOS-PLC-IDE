[package]
name = "plc-comms"
version.workspace = true
edition.workspace = true

[dependencies]
plc-core = { path = "../plc-core" }
thiserror = { workspace = true }
# EtherNet/IP - will add dependencies in Phase 4
tokio = { version = "1", features = ["net", "rt-multi-thread", "macros", "time"] }
bytes = "1"
